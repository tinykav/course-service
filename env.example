# Copy this to .env — never commit .env to Git!

# MongoDB Atlas connection string
# Get this from: MongoDB Atlas → Connect → Drivers → copy the URI
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/coursedb?retryWrites=true&w=majority

JWT_SECRET=your_super_secret_key_here

PORT=3000
